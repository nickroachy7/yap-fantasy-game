# Copy to .env.local and fill in. Client-safe values only.
# The balldontlie key is deliberately absent: it lives in Supabase Edge Function
# secrets and must never reach the client bundle.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
